program Example
const
  pi:double = 3.14567;
var
  num1, num2:integer;
  res, d:double;
begin
  num1:=5;
  write('Inter int number');
  read(num2);
  write('Inter double number');
  read(d);
  if(b mod 2 = 0) then
    begin
      Res:=(num1 - num2);
      write('Результат', Res);
    end
  else 
    write('Неверный код');
end.
